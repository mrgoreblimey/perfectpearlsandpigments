# WordPress GraphQL endpoint (requires WPGraphQL + WooGraphQL plugins).
# Leave unset to run the frontend on built-in seed data.
# WORDPRESS_GRAPHQL_URL=https://cms.perfectpearlsandpigments.co.uk/graphql
